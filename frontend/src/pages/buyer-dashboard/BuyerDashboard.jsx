import React, { useContext } from "react";

const BuyerDashboard = () => {
	return (
		<>
			<h1 className="mt-5 text-4xl text-center">Buyer Dashboard</h1>
		</>
	);
};

export default BuyerDashboard;
